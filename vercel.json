{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/assets/(.*)", "dest": "/frontend/$1" },
    { "src": "/(.*)", "dest": "/app.py" }
  ]
}
